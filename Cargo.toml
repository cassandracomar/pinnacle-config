[package]
name = "pinnacle-config"
version = "0.1.0"
edition = "2024"

[dependencies]
pinnacle-api = { git = "http://github.com/pinnacle-comp/pinnacle", branch = "main", default-features = false, version = "0.1.0" }

[features]
default = ["snowcap"]
snowcap = ["pinnacle-api/snowcap"]
